.gcloudignore
.git
.git/**
#!include:.gitignore

# Cloud Build needs only the application sources and root Docker build files.
# Exclude local planning/media artifacts so they can never enter build uploads.
.codex-tmp/**
docs/**
infra/**
scripts/**
services/rolecall-agent/tests/**
services/rolecall-agent/artifacts/**
